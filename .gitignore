# Environment files
.env
.env.local
.env.*.local

# Node modules
node_modules/
backend/node_modules/
frontend/node_modules/

# Mobile App build artifacts
mobile_app/.dart_tool/
mobile_app/build/

# Database and uploaded media
backend/data/*.db
backend/data/*.db-journal
backend/data/*.db-wal
backend/data/uploads/*
!backend/data/uploads/.gitkeep

# Auto-generated JWT & webhook secrets. Committing this file would leak the
# live secrets exactly the way 'Senin23062025' leaked — it must never be added.
backend/data/.secrets.json
docker/.env

# Build outputs
/dist/
!frontend/dist/**
.DS_Store
*.log

# Local CLI launchers & credentials
start-claude.ps1
start-claude*.ps1
